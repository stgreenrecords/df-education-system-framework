# DevOps Runtime Subdashboard

This is the live queue for `devops` implementation tasks. The main board remains the overall task source of truth.

| Priority | Task ID | Parent task | Title | State | Owner role | Affected scope | Blocked? | Last updated | Next action |
|---|---|---|---|---|---|---|---|---|---|
| P0 | TASK-012 | - | Automate Dark Factory launcher sessions and IntelliJ acknowledgements | BLOCKED | devops | root `call-*.bash` and `call-*.ps1` launchers, new DevOps automation helpers, IntelliJ/JetBrains workstation automation docs | Yes | 2026-05-28 12:15 local | Wait for external unblock: update/reconfigure the JetBrains/Copilot stack so the chat input becomes verifiable, or explicitly approve and provision a stronger desktop-automation dependency before SA reroutes the task. |
| P0 | TASK-010 | - | Add on-demand GitHub Actions AWS deployment pipeline | READY_FOR_QA | qa | `.github/workflows/deploy-aws-on-demand.yml`, `docs/deploy-aws.md`, `devops/kubernetes/platform-core/render-aws-deployment.ps1`, `devops/kubernetes/platform-core/README.md` | No | 2026-05-26 local | `qa` should validate the workflow structure, secrets wiring, render/deploy path, documentation coverage, and capture at least a dry-run Actions run if repository/AWS access is available. |
| P0 | TASK-009 | - | Create single file for Windows mac and Linux to start application | READY_FOR_QA | qa | root-level cross-platform local launcher, local runtime orchestration, startup/readiness output, related run documentation | No | 2026-05-26 local | QA should rerun the compose launcher and verify that PostgreSQL, the Spring backend, and `frontend/website` now start together, including website route/auth-proxy checks and cleanup. |
| P0 | STORY-023 | - | Define cloud-portable Kubernetes and IaC deployment baseline | DONE | factory | `devops/kubernetes/**`, `devops/iac/**`, deployment docs, provider overlays/modules | No | 2026-05-24 20:26 local | Accepted by PO |
| P0 | STORY-022 | - | Implement Podman-compatible OCI container baseline | DONE | factory | `devops/**`, runtime packaging, OCI image/build assets, local PostgreSQL container baseline | No | 2026-05-24 19:52 local | Accepted by PO |

## Lane notes

- 2026-05-28 12:15 local: `sa` reviewed the `TASK-012` blocker evidence and kept the task `BLOCKED` under `DECISION-026`. Do not start another DevOps rework cycle with the current workstation/tooling surface; resume only after the IDE/provider exposes a verifiable chat input or a stronger approved automation dependency is provisioned and SA reroutes the task.
- 2026-05-28 local: `devops` reworked `TASK-012` again by switching the GitHub Copilot fallback to the exact installed plugin actions `Copilot: Open Chat` and `New Chat Session`, adding focus-traversal retries plus fail-safe verification, and rerunning the supported live PowerShell path. Dry-run routing and parser validation still pass, and the live launcher now reaches deterministic Copilot actions without the earlier false-success path, but the current IntelliJ 2025.3.4 / Copilot widget still exposes no machine-verifiable prompt input after those actions run, so the task is now `BLOCKED` pending SA review of alternate approved automation options or an IDE/provider change.
- 2026-05-28 local: `qa` retested `TASK-012` after the keyboard-fallback rework and returned it to `devops` again. The earlier prompt-input exception no longer reproduced, but the latest live launcher run still failed the acceptance criteria because the fallback pasted `pickup new task and start to work` into IntelliJ Settings -> Plugins search instead of the JetBrains AI chat surface while the orchestrator log still claimed success. DevOps must make the fallback deterministic and verify the actual target before reporting prompt submission.
- 2026-05-28 local: `devops` completed the `TASK-012` QA-return rework and moved the task back to `READY_FOR_QA`. UIAutomation still cannot see a JetBrains AI prompt input on this IntelliJ 2025.3.4 workstation (`SunAwtFrame` exposes only `JBRCustomTitleBarControls`), so delivery added a repository-owned keyboard fallback in `devops/automation/jetbrains-agent-orchestrator.ps1` and `jetbrains-agent-config.json`. The supported live PowerShell launcher paths for both `call-start-factory.ps1` and `call-dev-devops.ps1` now complete successfully and log keyboard-fallback prompt submission instead of throwing the previous prompt-input exception.
- 2026-05-28 local: `qa` returned `TASK-012` to `devops` after reproducing a live failure on the supported PowerShell launcher path. `./call-start-factory.ps1 -DryRun -NoWatcher` still resolved the correct initial prompt, but live `.\call-start-factory.ps1` started the watcher and then threw `Could not find a JetBrains AI prompt input element...`. QA also observed that a UIAutomation diagnostic against the active IntelliJ window exposed only one visible descendant (`ControlType.Pane` / `JBRCustomTitleBarControls`), so DevOps must harden prompt submission beyond the current input-element discovery path.
- 2026-05-26 local: `devops` added PowerShell-native launcher support for `TASK-012` after user feedback confirmed the JetBrains terminal was running PowerShell, where `./call-start-factory.bash` is intercepted by Windows file association before Bash executes. Delivery added root `call-*.ps1` launchers plus `devops/automation/call-jetbrains-agent.ps1`, documented `.ps1` as the supported PowerShell path, and validated PowerShell dry-run launcher routing alongside the existing Git Bash checks.
- 2026-05-26 local: `devops` hardened `TASK-012` after user feedback that `./call-start-factory.bash -DryRun -NoWatcher` opened Visual Studio Code on one workstation. Root cause is most likely Windows/editor `.bash` file-association behavior on the nested helper path, so all root launchers now invoke `devops/automation/call-jetbrains-agent.bash` through the active Bash interpreter explicitly; the exact reported dry-run path and the full Git Bash launcher suite were rerun successfully after the fix.
- 2026-05-26 local: `devops` completed `TASK-012` and moved it to `READY_FOR_QA`. Delivery wired the root `call-*.bash` launchers through `devops/automation/call-jetbrains-agent.bash`, implemented `devops/automation/jetbrains-agent-orchestrator.ps1` plus `jetbrains-agent-config.json` for repository-owned prompt routing and the explicit `Keep All` / `Add to Git` / `Continue` watcher allowlist, documented operator usage/recovery in `docs/jetbrains-dark-factory-automation.md`, and passed dry-run Git Bash wrapper/orchestrator validation plus parser/syntax checks.
- 2026-05-26 local: SA routed `TASK-012` to `devops` from the user's urgent Dark Factory framework automation request. The recommended implementation is a Windows-first Bash-wrapper + PowerShell desktop-automation flow that starts the initial `pickup new task and start to work` prompt from `call-start-factory.bash`, starts role-specific prompts from the existing root launcher scripts, and safely auto-clicks the IntelliJ/JetBrains AI buttons `Keep All`, `Add to Git`, and `Continue`.
- 2026-05-26 local: `devops` completed `TASK-010` and moved it to `READY_FOR_QA`. Delivery added the manual GitHub Actions AWS deployment workflow, the AWS runbook, and a reusable AWS manifest render helper; local validation passed for workflow YAML structure, Maven packaging, baseline AWS overlay rendering, and offline manifest rendering with/without an IRSA role ARN, while live Docker build and GitHub/AWS execution remained environment-limited on this workstation.
- 2026-05-26 local: SA routed `TASK-010` to `devops` from the user's explicit request for an on-demand AWS deployment pipeline using GitHub Actions plus secrets `AWS_ACCESS_KEY` and `AWS_SECRET_KEY`. The recommended implementation is a manual `workflow_dispatch` flow that builds the accepted `backend/platform-core` OCI image, pushes it to ECR, renders the existing AWS Kubernetes overlay with deployment-time values, and deploys it to EKS without committing country-specific settings.
- 2026-05-26 local: `devops` completed rework for `TASK-009` and moved it back to `READY_FOR_QA`. `compose.local.yaml` now includes a containerized `frontend` service for `frontend/website`, published website port overrides, and frontend dependency-cache volumes; local validation passed for backend health, OpenAPI, website routes, frontend auth-proxy login, and cleanup.
- 2026-05-26 local: `po` rejected `TASK-009` and returned it to `devops` after explicit human product feedback confirmed that the one-command launcher is expected to start the website frontend too. The current `compose.local.yaml` starts only PostgreSQL plus the Spring backend, which is no longer sufficient for product acceptance.
- 2026-05-26 local: `qa` completed independent verification of `TASK-009` and moved it to `READY_FOR_PO`. QA confirmed that `compose.local.yaml` is the single launcher file, that it starts PostgreSQL plus the Spring backend successfully, that cleanup works, and that the missing website process is intentional/documented rather than a defect in the current task scope.
- 2026-05-26 local: `devops` completed `TASK-009` and moved it to `READY_FOR_QA`. Delivery replaced the discarded Java launcher draft with one terminal-first cross-platform `docker compose` file, `compose.local.yaml`, which starts PostgreSQL plus the Spring backend from the real repository source/module path. Local validation passed for compose startup, backend health, OpenAPI, bootstrap-admin login/MFA-challenge behavior, and compose cleanup.
- 2026-05-26 local: `devops` started `TASK-009` and corrected the implementation direction after explicit user feedback that the launcher must be a terminal-first startup file rather than a Java main-class wrapper. The lane replaced the earlier draft with one cross-platform container-orchestration file focused on Spring + PostgreSQL startup from terminal commands.
- 2026-05-26 local: SA routed `TASK-009` to `devops` from an explicit user request for one startup file that works across Windows, macOS, and Linux. The recommended solution was initially a single Java source launcher, but implementation could adapt if a stronger single-file terminal-first approach proved better within DevOps scope.
- New DevOps implementation tasks must be added here before `devops` starts work.
- DevOps implementation notes belong under `df/artifacts/{task-id}/devops/`.
- Do not track design, backend, frontend, or data-engineering tasks on this subdashboard.
- `STORY-023` is the current active Phase 1 deployment-baseline task and must preserve the provider-neutral Kubernetes base vs provider-specific overlay/module split documented by SA.
