# Dark Factory Risks and Blockers

| ID | Risk/blocker | Severity | Owner | Status | Mitigation |
|---|---|---|---|---|---|
| RISK-001 | Poland education research may be incomplete | High | PO/SA | Open | Use multiple official sources; validate with domain expert |
| RISK-004 | Scope creep across 20 epics may delay MVP | Critical | PO | Open | Strict phase boundaries; PO acceptance gates |
| RISK-007 | Data sovereignty vs centralized AI services conflict | High | SA | Open | Design AI as pluggable; country chooses provider |
| RISK-010 | DB migrations must be robust for country-operated deployments | High | SA/Dev | Open | Automated Flyway migrations; rollback scripts; compatibility checker |
| RISK-011 | Poland higher-education grading may be institution-specific rather than nationally uniform | Medium | SA/Dev/PO | Open | Model higher-education grading as institution-level configuration; validate with pilot universities before implementation |
| RISK-012 | Poland school-year dates and winter breaks vary by academic year and voivodeship | Medium | PO/QA | Open | Treat school calendar as versioned yearly data and validate against annual ministry notices before coding |
| RISK-013 | Translation cache invalidation may need a distributed provider in multi-node deployments | Medium | SA/Dev | Open | Implement behind Spring Cache-compatible abstraction; start local for MVP and allow Redis/provider swap through configuration |
| BLOCKER-014 | STORY-220 was blocked because database migration framework and PostgreSQL configuration were not yet available in the repository | Critical | human/factory | Resolved 2026-05-24 | `STORY-011` is now PO-accepted; SA should resume STORY-220 and reroute the retired `dev` ownership to the active delivery lane before implementation continues |
| RISK-015 | Deferring containerization until after major feature work would create deployment rework in configuration, secrets, health checks, filesystem assumptions, and cloud portability | High | SA/devops | Open | Treat containerization as Phase 1: keep STORY-010/011 container-ready, then implement STORY-022 Podman-compatible OCI baseline and STORY-023 Kubernetes/IaC baseline before deep feature work |
| RISK-016 | Local Java/Maven trust store may fail Maven Central TLS validation on some developer machines | Medium | delivery lanes/QA | Open | QA should rerun `.\mvnw.cmd clean verify` in a normally trusted environment; affected machines need their Java trust store fixed or a documented local trust-store workaround |
| RISK-017 | Frontend and DevOps Maven projects are currently structural scaffolds only | Low | SA/frontend-dev/devops | Open | Future frontend and deployment stories must add actual tooling, packaging, and deployment behavior inside the existing independent project boundaries |
| RISK-018 | Existing active tasks may still refer to the retired generic `dev` owner after the lane split | Medium | SA/QA | Open | New work must use `designer`, `backend-dev`, `frontend-dev`, `devops`, or `data-engineer`; existing active tasks should be completed or explicitly migrated when resumed |
| RISK-019 | Parallel lane tasks may still conflict on root build, CI, runtime, or shared acceptance-criteria files | High | SA/delivery lanes | Open | SA must split scopes, document affected files, and sequence work when shared files or environments are involved |
| RISK-020 | Website, Android, and iOS frontend projects may drift or create hidden source coupling | Medium | SA/frontend-dev/QA | Open | Keep projects independently buildable; share only through APIs, generated clients, design tokens, or explicit shared packages approved by SA |
| RISK-021 | Mobile frontend work may distract from the website-first frontend foundation if promoted too early | Medium | PO/SA/frontend-dev | Open | Keep Android/iOS as last-priority stories unless PO/SA documents a promotion reason |
| RISK-022 | Springdoc OpenAPI dependency compatibility with the current Spring Boot 4 version must be proven locally | Medium | backend-dev/QA | Open | Use Springdoc 3.x Spring Boot 4 documentation as implementation reference and require integration tests for `/api-docs` and `/swagger-ui` |
| RISK-023 | UI-facing frontend work may be attempted without designer input | Medium | designer/frontend-dev/QA | Open | Frontend-dev must block missing design packages; QA must verify design evidence before passing UI work |
| RISK-024 | Country seed/test datasets may mix true public facts with real personal records or unsourced values | High | data-engineer/QA | Open | Require source maps for real city/district/school/subject names and synthetic teacher/student/grade records only |
| RISK-025 | Automated PostgreSQL integration tests for STORY-011 may depend on a compatible isolated database runtime on developer/QA machines | Medium | backend-dev/QA | Open | Prefer isolated automated PostgreSQL testing with a compatible local container runtime or documented equivalent; keep datasource configuration environment-driven and avoid hard dependency on one manually prepared shared DB |
| RISK-026 | STORY-220 may need a temporary local translation-audit store before the broader platform audit subsystem exists | Medium | SA/backend-dev | Open | Keep the translation audit contract generic and migration-friendly so later audit consolidation does not require translation behavior changes |
| RISK-027 | STORY-022 requires Podman-compatible OCI workflows, but some developer/QA environments may have Docker or another OCI runtime available instead of Podman | Medium | SA/devops/QA | Open | Keep all container assets OCI-compatible and Podman-first, avoid Docker-daemon-specific assumptions, and document any runtime-substitution evidence when Podman is unavailable locally |
| RISK-028 | STORY-023 Kubernetes/OpenTofu baseline validation may be limited by missing local `kubectl`, `kustomize`, local-cluster, `tofu`, or `terraform` tooling in developer/QA environments | Medium | SA/devops/QA | Open | Keep the baseline renderable with open tooling, define fallback validation paths up front, and require explicit documentation of any missing-tool limitations rather than silently skipping checks |
| RISK-029 | STORY-030 configuration inheritance may sprawl into full compatibility reporting or hard-coupled organization modeling before the MVP foundation is stable | High | SA/backend-dev | Open | Keep the first implementation limited to generic scope-path resolution, override/lock validation, and `REPLACE`/`EXTEND_SET` merge behavior; defer compatibility reporting to `STORY-031` and authoritative hierarchy persistence to later organization work |
| RISK-030 | STORY-013 audit payloads may grow large or expose sensitive before/after values if the generic audit model is too loose | High | SA/backend-dev/QA | Open | Keep the first audit schema append-only and generic but bounded, avoid verbose payload logging, require filtered query/export coverage, and leave advanced masking/retention policies as explicit follow-up work |
| RISK-031 | STORY-082 administrator MFA may accidentally expose TOTP secret material or create a privileged pre-MFA session during enrollment/verification if the challenge flow is designed too loosely | High | SA/backend-dev/QA | Open | Use short-lived purpose-bound MFA challenge tokens only, encrypt stored TOTP secrets with an externalized key, avoid logging secrets/challenge payloads, and require focused integration tests for enrollment-required vs verification-required branches |
| RISK-032 | Flattening root design assets to `design/{page-slug}/` may cause folder-name collisions across tasks if page slugs are not uniquely chosen | Medium | SA/designer/QA | Open | Require globally unique descriptive page slugs in designer guidance, keep exact asset references in each task package/handoff, and have QA verify path clarity when reviewing design tasks |
| BLOCKER-033 | `TASK-006` frontend validation cannot run on this workstation because the shell environment does not expose `node`, `npm`, `npx`, or `corepack` | High | human/factory | Open | Resume `frontend-dev` on a workstation with Node.js 20+ and npm available, then run `frontend/website` install/lint/typecheck/build/manual login-flow validation before moving the task forward |
| BLOCKER-034 | `TASK-012` cannot satisfy the live launcher acceptance criteria on the documented Windows + IntelliJ IDEA 2025.3.4 + GitHub Copilot baseline because the Copilot chat surface exposes no positively verifiable prompt target after the exact `Copilot: Open Chat` / `New Chat Session` actions run | Critical | human/external-system | Open | Update/reconfigure the IDE/plugin stack so the chat input becomes verifiable, or explicitly approve and provision a stronger desktop-automation dependency before SA reroutes the task |

See `df/backlog/risks-and-assumptions.md` for the full risk register.
