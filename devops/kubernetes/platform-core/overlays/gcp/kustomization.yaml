apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base
  - ingress.yaml

patches:
  - path: provider-patch.yaml

commonAnnotations:
  deployment.darkfactory.io/provider-target: gcp

